{
  "name": "lab-db",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"pnpm run server\" \"pnpm run client\"",
    "server": "uvicorn server_py.main:app --port 3001 --reload",
    "client": "cd client && pnpm run dev"
  },
  "dependencies": {
    "concurrently": "^9.0.0"
  }
}
